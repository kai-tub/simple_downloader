name: Test
on:
  push:
    branches: [main]
    paths-ignore:
      - "simple_downloader/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "simple_downloader/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
